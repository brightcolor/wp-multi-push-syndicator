# Scheduling Strategies

Supported strategy keys:

- `fixed_delay`
- `random_delay`
- `preferred_time`

Minimum delay is always enforced and never bypassed.

Preferred-time mode auto-selects next valid future slot if a preferred slot becomes invalid.