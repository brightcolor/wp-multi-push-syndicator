# Rewrite Extension

Current release intentionally ships with no AI rewrite integration.

Use transformer interface and hooks to add your own rewrite provider.

- `wmps_register_transformers`
- `wmps_transform_payload`